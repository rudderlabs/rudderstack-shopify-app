apiVersion: v1
kind: Service
metadata:
  name: {{ include "shopify-app-chart.fullname" . }}
  labels:
    {{- include "shopify-app-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shopify-app-chart.selectorLabels" . | nindent 4 }}
